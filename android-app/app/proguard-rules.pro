# Minify is off for this first version (see app/build.gradle.kts —
# isMinifyEnabled = false), so this file has no effect yet. Left in place
# because Android Studio's project template expects it to exist, and it's
# the right place to add rules later if minification is turned on for a
# release build.
